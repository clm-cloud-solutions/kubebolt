services:
  api:
    build: ../apps/api
    ports: ["8080:8080"]
    volumes:
      - /tmp/docker-kubeconfig:/root/.kube/config:ro
      - ~/.aws:/root/.aws:ro
    extra_hosts: ["host.docker.internal:host-gateway"]
  web:
    build: ../apps/web
    ports: ["3000:3000"]
    depends_on: [api]
