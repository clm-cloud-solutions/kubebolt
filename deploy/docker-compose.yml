version: "3.8"
services:
  api:
    build: ../apps/api
    ports: ["8080:8080"]
    volumes: ["~/.kube/config:/root/.kube/config:ro"]
  web:
    build: ../apps/web
    ports: ["3000:3000"]
    depends_on: [api]
